# September-4 extraction and source-review receipt

## Frozen identities

- PIR execution commit: `461bf8e70380eae700a61e201fc4de66719c9d9d`
- Study OS source repository: `Pukujan/Study-os`
- Study OS source commit: `bef88216d9052eeb7e135916ea7fb6f8d84e0ccc`
- Extraction revision: `sep4-full-accounting.v0.1`
- Source-review ledger: `reviews/sep4/semantic-ledger.v0.json`
- GitHub Actions run: `33978407467`
- Receipt artifact ID: `9973007992`
- Receipt ZIP SHA-256: `e36dfb7f61348ebe01c0051faa7626781a38b1930dedaa776d87bafa8d7f4cd6`

## Deterministic source-accounting result

- raw transcript parts: **8**
- parsed turns: **887**
- exact spans: **887**
- learner turns: **443**
- tutor turns: **444**
- disposition records: **887**
- unique disposed source turns: **887**
- missing turns: **0**
- duplicate turns: **0**
- unknown turns: **0**
- coverage complete: **true**

All 887 primary dispositions remain `UNRESOLVED` in the accounting ledger. This is intentional: accounting coverage is not semantic understanding.

## Source-reviewed semantic candidate

The unchanged human source-review ledger resolved successfully against the generated turn manifest:

- candidate pedagogical events: **36**
- explicit failure → repair regions: **18**
- unresolved questions retained: **4**
- evidence references that failed to resolve: **0**

The candidate trajectory includes the calibrated bridges and constraints around full-problem anchoring, position/index, k/box, sum recurrence, `enumerate(a)`, append conversion, Python-loop bridging, representation-preserving validation, `max_sum`, `S[0] → S[i]`, explicit-index → symbolic comparison, max-loop construction, `if i != 0 → else`, boundary/break, arbitrary `k`, `range(k)`, `x`, same-`S[i]` accumulation, nested-state charts, and final-loop exposure without a mastery claim.

## Unresolved by design

1. Whether the earlier `max(S)` detour belongs in the executable golden or is superseded exploration.
2. Whether transcript-faithful `len` ordering should remain distinct from a later improved experiment.
3. How the early part-01 teaching attempts should be dispositioned relative to the later calibrated path.
4. Final combined-loop exposure must not be promoted into independent learner mastery without evidence.

## What this proves

This receipt proves that the pinned eight-part source bundle can be parsed byte-exactly into a complete 887-turn manifest, and that every evidence range in the current 36-event/18-repair source review resolves to real source turns.

## What this does NOT prove

It does **not** prove that:

- the 36-event candidate is the final canonical golden;
- every one of the 887 turns has been semantically dispositioned;
- the current experimental vertical IR can represent the whole trajectory;
- an LLM can discover this trajectory from a new problem;
- hidden/prospective holdouts pass;
- learner efficacy generalizes beyond this calibration session.

## Schema-pressure finding

The full-source review already falsifies a generalization of the current max-sum experimental IR: `VerticalRepresentation` requires a window `box`, while the calibrated `enumerate(a)` representation is an index/number/pair chart and does not have a window box. The experimental max-sum schema therefore MUST NOT be promoted unchanged into canonical G2 PIR.

Next proof: use the calibrated `enumerate(a)` segment as a second vertical slice. The smallest evidence-driven change is to permit non-window chart representations, then require the same-chart validation and answer-leak constraints to survive that slice before any broad G2 schema promotion.
