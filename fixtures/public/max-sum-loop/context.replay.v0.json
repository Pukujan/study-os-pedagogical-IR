{
  "schema_version": "pir.experimental-replay-context.v0",
  "trajectory_id": "sep4.max-sum-loop.replay.v0",
  "persistent_text": [
    "max_sum has been initialized from the first window and the first concrete comparison has been validated.",
    "Build the max loop from fixed-index comparison code, then generalize to S[i], then assemble the standalone loop.",
    "Do not combine the window-sum loop and max loop in this slice."
  ]
}
